# 1cenc 基本流程使用教程

本文档面向入门群体，用于说明从下载到开始压制视频的完整流程顺序；本文的操作流程会写的偏详细，且会忽略可能与压制性能相关的使用建议。

## 如何反馈问题

通过 [GitHub Issues](https://github.com/iAvoe/OneColumnEncoder/issues) 或 [NazoRip 项目评论区](https://nazorip.site/archives/1593/) 可以反馈。反馈前请确认问题着实归咎于 1cenc，最好有截图、运行日志拷贝、ffprobe 日志之类的辅助信息，以便排查。

## 下载与“安装”

运行 1cenc 需要至少 Windows 10 系统，建议 1809 / 21H2（LTSC）或更高版本，最低 1607
- 64bit 操作系统下载 `1cenc-win-x64.7z`
- 32bit 操作系统下载 `1cenc-win-x86.7z`
- 不推荐在缺少折腾意愿的情况下使用 32bit 版本

> 主流操作系统皆为 64bit，并且部分集成的工具不支持 32bit 操作系统，因此尽管能跑，但功能有缺

### 下载与“安装”——解压文件列表

<img src="./img-cn/1-Extract-Archive.png" alt="解压文件列表" width=300 />

- `1cenc` 文件夹：集成的工具、队列列表数据、压制运行日志、设置等数据
- `OneColumnEncoder.exe`：主程序
- `OneColumnEncoder.pdb`：崩溃除错信息（反馈问题时上传）
- `x64-upstreams-encoders.7z`：ffmpeg、ffprobe、x264 等**视频处理工具**的压缩包
    - 这些工具体积太大（触发 GitHub 限制），因此需要额外进行解压
    - 32bit 操作系统则为 `x86-upstreams-encoders.7z`

**集成的视频处理工具**
- `ffmpeg.exe`：负责视频解码、ffmpeg 滤镜、视频帧压制预览、重分集分界预览、自动封装
- `ffprobe.exe`：负责分析视频源（生成部分编码器参数、检查格式兼容性以及是否损坏）
- `x264[...].exe`：AVC 视频编码器
- `x265[...].exe`：HEVC 视频编码器
- `SvtAv1EncApp[...].exe`：AV1 视频编码器

> 附带所有工具的 1cenc 会超过 350MB，主因是 FFmpeg 和内置 .net 9.0 的依赖所增，轻量安装可询问 AI 来“自行”编译
> 工具详细说明见视频压制相关工具下载教程：[NazoRip](https://nazorip.site/archives/1482)；[GitHub](https://github.com/iAvoe/encoding-tools-download-tutorial)

**其它工具**
- `x64-AVS-VS-plugins`：AviSynth、VapourSynth 视频滤镜（需安装和导入 AviSynth、VapourSynth）
- `x64-CloudinarySSIMULACRA2.1|GoogleButteraugli`：集成的图像画质跑分工具，用于预览视频压制参数设置的影响

#### 视频处理工具放置

`x86|x64-upstreams-encoders.7z` 中的 .exe 工具可以放在任意位置，但 1cenc 拥有自动导入功能，且会搜索以下范围，让后续的导入操作更轻松：

>     .
>     ├── ①
>     ├── OneColumnEncoder.exe
>     ├── OneColumnEncoder.pdb
>     ├── x64-upstreams-encoders
>     │    └── ②
>     └── 1cenc
>          ├── ③
>          ├── x64-upstreams-encoders
>          │   └── ④
>          ├── x64-AVS-VS-plugins
>          │   └── ...
>          ├── x64-CloudinarySSIMULACRA2.1
>          │   └── ...
>          └── x64-GoogleButteraugli
>             └── ...
>

---

## 首次启动

双击 `1cenc.exe` 以打开。首次启动会询问是否自动导入，点击确认导入：

<img src="./img-cn/2-Auto-Import.png" alt="自动导入提示" width=400 />

若点错或遗漏，则可以手动导入，或在设置界面勾选“下次打开时运行自动导入”

<img src="./img-cn/3-Rerun-Auto-Import.png" alt="重运行自动导入" width=1000 />

> 若希望在别处也能打开 1cenc，可以创建快捷方式或开始菜单项。

由于 VapourSynth 和 SVFI 是安装到系统的软件，因此 1cenc 的自动导入搜索会尝试找出它们并导入。下图中显示了自动导入将 4 个解压出的工具，以及 2 个已安装的工具同时导入的样式。

<img src="./img-cn/4-Import-Result-And-Click-Select.png" alt="导入效果" width=550 />

### 首次启动——主界面一览

1cenc 的主界面主要由**标题**、**检查列表**、**卡片**和**按钮**组成。其中工具卡片根据功能不同，点击后可能会选中或打开窗口。

#### 工具卡片

主界面的主要组成部分。由黑框、黑字和浅灰背景组成。拥有展开/折叠、悬浮高亮、选中和异常状态显示。熟悉操作后，折叠状态可以让操作更简便。
- 若工具状态异常情况（边框变红）出现，则重新导入工具

<img src="./img-cn/5-Tool-ItemCard.png" alt="工具卡片" width=1000 />

由于“路径”太长，工具卡片提供了两种完整路径显示功能：
- 光标悬浮一段时间后的工具提示
- 点击选择工具卡片后，在下方的提示文本中

<img src="./img-cn/6-Hover-Hint.png" alt="悬浮提示" width=350 />

#### 标题、按钮

**标题**说明了操作步骤顺序，步骤说明，以及包含了右侧的展开/折叠按钮。

<img src="./img-cn/7-Main-Page-Title-Buttons.png" alt="标题和按钮" width=550 />

**按钮**会执行一些操作或打开窗口，但若未满足运行前提时，按钮会被禁用

> 其中“3. 选择依赖文件”用于 AvsToPipeMod 的 AviSynth.dll 依赖，不用则无视。

#### 检查列表

各种检查项目，包括是否选择了工具，满足解锁“开始压制”按钮的条件，视频源可能存在的问题等。
- 检查列表也有折叠状态
- 点击列表项以查看详情

<img src="./img-cn/8-Checklist.png" alt="检查列表" width=700 />

**检查项的 4 种状态：**
- 未查——黑字、闭眼图标：检查项与当前流程不相关，被忽略
- 通过——绿字、对勾图标：正常或条件已满足
- 警告——黄字、双叹号图标：可能会导致压制结果异常，但不禁用“开始压制”按钮
- 错误——红字、打叉图标：基础条件不满足或无法启动压制的异常，禁用“开始压制”按钮

#### 视频源卡片、压制配置卡片

这些卡片的功能不同，因此外观区别于默认的白底黑字。视频源卡片的普遍点击行为是打开文件选择窗口；压制配置卡片的普遍点击行为是打开配置窗口。

<img src="./img-cn/9-Source-And-Config-ItemCards.png" alt="视频源与配置卡片" width=1300 />

**视频源卡片的功能：**
- **视频源**：选择一个视频源
- **视频队列（Queue）**：选择多个视频源或蓝光播放列表（PLAYLIST）文件夹
- **视频源拼接（Concat）**：选择多个视频源，输出一个拼接后的视频
- **视频源重分集（Repart）**：选择视频源文件夹（多个视频源）或蓝光播放列表（PLAYLIST）文件夹，指定分割线位置，输出多个切分或合并后切分的视频（可以简单视为切分）

---

### 首次启动——设置一览

设置页是 1cenc 中打开最慢的窗口——因为字体设置的加载所致。

<img src="./img-cn/10-Settings.png" alt="设置" width=500 />

#### 默认与建议值

**文件覆盖确认——冷却兆字节除数**
- 当检测到将被覆盖掉的文件时，根据将被覆盖文件的大小来算冷却时长
- 建议保持默认，防止大文件被轻易覆盖，最小值为 1

**编码日志 TXT**
- 将选择的上游程序（ffmpeg、VapourSynth、...）和下游程序（x264、x265、SVT-AV1）在压制任务中产生的运行日志（Runtime log）保存到文件
- 建议开启以便排查，文件数量上限的默认值是根据常见的番剧集数（即队列长度）而定

**自动封装开关**
- 将压制出的视频流封装为 `.mkv` 格式
- 由于 x265 不支持自动封装（只导出 `.hevc`），且会导致帧率等元数据遗失，因此建议为其开启
- x264 会封装到 `.mp4` 格式，SVT-AV1 会封装到轻量的 `.ivf` 格式，这些格式存在音频兼容性限制，因此关闭后会禁用音频处理机制

**自动封装——音频处理**
- 由于 `.mkv` 格式良好的兼容性，也被 Adobe 系列剪辑软件支持，因此**单文件**和**队列模式**推荐直接拷贝音频流到自动封装结果
- **合并模式**和**重分集模式**涉及音频流的剪辑，有的音频流可能不支持或支持不良，因此默认值设为重编码
- 尽管重编码模式有 Opus 格式支持，但默认值故意设置为 AAC 格式，以求兼容

---

## 首次压制

1. 点选 FFmpeg
2. 点选 x264
3. 选择一个视频源
    - 每次选择新视频源时，1cenc 会自动运行视频分析，否则需要点击“运行视频源分析”按钮
    - FFProbe 会被自动选中，因此无需手动点选
4. 点击“开始压制”
    - 当然，可以考虑点击“输出文件名与路径”来调整输出位置

<img src="./img-cn/11-Start-First-Encode.png" alt="启动首次压制" width=600 />

点击“开始压制”后，1cenc 会首先弹出一个压制和封装参数对话框，该对话框支持右键复制文本。
- 图中是**压制**→**封装**的两段式结构
    - 即“**视频压制**→**音频拷贝或压制 + 封装**”
- 在**合并模式**、**重分集模式**，并且设置自动封装音频处理为“重编码到 AAC/Opus”时，编码命令会变成 3 段式结构
    - 即“**视频压制**→**音频压制**→**封装**”
- 如果看到 `--master-display` 或 `G(xxx,yyy),B(zzz,aaa),R...` 值的参数，则说明源视频的 HDR 元数据附录/侧数据（sidedata）已经被识别和转换到压制参数

<img src="./img-cn/12-Inspect-Encoding-Param.png" alt="检查参数" width=400 />

再次点击确认后，主界面会隐藏以释放内存和减少背景干扰，**压制监视器**窗口会打开：

<img src="./img-cn/13-Encoding-Monitor.png" alt="压制监视器" width=800 />

### 首次压制——压制监视器一览

**进度条**
- 如果视频元数据中存在视频总帧数，则显示进度，否则显示理发店灯柱动画（完成时动画停止）

**内存占用**
- 检查当前电脑的内存大小是否存在瓶颈
- 检查是否应该关闭一些程序来释放更多内存
- 如果电脑有多个 NUMA 节点，则理论上还可以打开几个压制进程

**进程日志**
- 左侧为上游程序（ffmpeg、VapourSynth、...）日志
- 右侧为下游程序/编码器日志
- 中间的分隔线可以拖动，以扩大和缩小两个窗口
- 底部的三个按钮可以复制当前日志文本，或轮换字号

**时间计算器**
- 查看用时，预计完成时间等时间状态
- 如果视频元数据中不存在视频总帧数，则预计相关的时间信息不做更新

**富文本解析**
- 识别字体、颜色等命令符号（SVT-AV1 会写）

**保存日志**
- 将此次压制产生的进程日志保存为 `1cenc` 目录下的 `.txt` 文件

**浏览与进程操作**
- **中断上游/下游程序**
    - 上游程序负责解码，因此完成的帧数比下游程序（编码器）更多一些
    - 选择取决于是否多压制几十帧（中断上游）还是急停（中断下游）
- **关闭**
    - 在上游程序和下游程序皆中断/退出后解锁

在编码完成后，1cenc 会启动自动封装操作，从而产生第二段上游程序的日志内容：

<img src="./img-cn/14-Encoding-Monitor-On-Fin.png" alt="压制监视器完成状态" width=800 />

---

## 再次压制（完整流程版）

***依照下图的配置/点击顺序重新进行此次编码：***

<img src="./img-cn/15-Start-First-Encode-Again.png" alt="再启动首次压制" width=600 />

### 再次压制 ④——滤镜编辑器一览

滤镜编辑器中的一些命令行/脚本行基于分析数据生成，因此它的按钮只有在完成了“运行视频源分析”后才会解锁。
- 滤镜编辑器会自动选择标签页（此处上游程序为 ffmpeg）

<img src="./img-cn/16-Filter-Scribe-Modal.png" alt="滤镜编辑器" width=500 />

图中大部分滤镜参数都是“N/A”——因为视频源不需要这些修复，反之如果视频源是可变帧率、非方形像素（需修复 SAR）等情况，则会生成相应的滤镜命令行。

***依照下图控制分辨率缩小命令，生成一个缩小命令，然后粘贴到顶部的滤镜参数框里，点击确认。***

<img src="./img-cn/17-Filter-Scribe-Modal-Apply.png" alt="滤镜编辑器" width=500 />


点击确认保存时，会有一个“覆盖 ffprobe JSON”的确认框出现，这是因为滤镜会修改视频源的分辨率，导致压制参数构建的依据变更，而需手动校准一次。

***点击“更新 ffprobe JSON”。***

<img src="./img-cn/18-Source-Reviser.png" alt="元数据修改器" width=350 />

> 注：VapourSynth 滤镜编辑器支持单帧滤镜效果预览

### 再次压制 ⑤——复制原生 JSON

当 1cenc 做出了奇怪的检查列表判断，或者触发预期外的警告时，可以通过它来排查。

***（可选）打开熟悉的文本编辑器，粘贴进去检视一下***

### 再次压制 ⑥——输出文件名与路径

用于预览文件名在各种设备/UI 宽度限制下的最终呈现效果，同时避免因文件名导致的压制失败可能，对于管理大量文件，或有发布需求的情况有用。

在此设定一个新的文件名，并指定新的输出位置

<img src="./img-cn/19-Output-Filename-And-Path.png" alt="输出设置" width=350 />

### 再次压制 ⑦——并行计算调度

在此设置上游程序和编码器具体运行在哪个 NUMA 节点，在使用超过一路 CPU—内存节点的电脑，因为 BIOS 设置，或者使用 TR（线程撕裂者）、EPYC（霄龙）处理器时，就有概率暴露出超过一个节点的选项。

**限制上 | 下游程序线程到物理核心数**
- 编码器（下游程序）是重计算、轻缓存的程序（尽管缓存量取决于视频分辨率）
    - 限制到物理核心数后可以达成“HPC 关超线程策略”的提速，同时不浪费超线程所带来的“轻计算、重缓存”程序的低占用
- 这决定了 1cenc 比一般压制软件更快，更稳定

**管道缓冲区**
- 通过增加缓存占用，避免性能瓶颈——理论上是这样，实际测试中没有显著变化，也没有负面影响，可以忽略。

***此处可以维持默认或勾选全部项目，然后确认***

<img src="./img-cn/20-Parallelism-Settings.png" alt="并行计算调度" width=500 />

### 再次压制 ⑧——压缩参数配置（和预览）

在此设置和预览编码器/下游程序参数：
1. 根据（CRF 模式标签页底部的）**CRF 刻度**提示选一个合适的 CRF 值
2. **自定义参数——基础参数**中选一个和视频源相关性最高的预设
3. **自定义参数——关键帧距离秒数**中根据下方提示选一个符合播放工况的值
4. （忽略其他设置项）
5. 在右侧预览窗底部点击**预览**，然后等一阵子
    1. 等待当前帧编码完成
    2. （可选）等待右下角蓝字的 SSIMULACRA、Butteraugli 画质指标跑分运行完成
6. 拖动 SOURCE 和 ENCODE 之间的分隔线，从而精确对比二者
7. （可选）拖动**画面位置**进度条，再次预览
8. （可选）更换预览窗左上角的**编码器**，再次预览

***根据此处说明调整 CRF 模式参数值、选择基础参数、关键帧距离后点击左栏底部的确认***

<img src="./img-cn/21-Encode-Settings.png" alt="压制设置" width=1200 />

### 再次压制——取段打样

指定压制视频的一段。此功能除了清理多余的片头片尾，也应对了有分享或者样品考验需求（如压制组内审核流程），证明**主观画质正常**的用途。

***尝试用时长滑条扩大片段到 60 秒，拖拽时间轴的黄色部分到片头，点击“开始打样”***

<img src="./img-cn/22-Clip-Sampler.png" alt="取段打样" width=450 />

在确认“开始打样”后，同样会弹出一个调试窗口，不过这次不会再出现封装命令了。

***点击确认，开始压制***

<img src="./img-cn/23-Inspect-Sampling-Param.png" alt="检查打样" width=400 />

---

## 基本流程教程完

### 未提及内容（高级用例）

- 蓝光章节导入
- 视频源队列模式
- 视频源拼接模式
- 视频源重分集模式（基本分割、肉酱盘处理、合并盘处理）
- 基本 AviSynth 和 VapourSynth 滤镜处理与队列、拼接、重分集模式的变化
- 多 NUMA 节点的多开策略（标题 PID 号、源文件夹拆分、队列拆分）